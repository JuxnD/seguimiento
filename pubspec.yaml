name: seguimiento
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  drift: ">=2.19.2 <2.23.0"
  file_picker: ">=8.0.0 <9.0.0"
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  http: ">=1.2.0 <2.0.0"
  package_info_plus: ">=8.0.0 <9.0.0"
  path: ^1.9.0
  path_provider: ^2.1.5
  share_plus: 10.1.4
  sqlite3: ^2.4.7
  sqlite3_flutter_libs: ^0.5.42
  url_launcher: ">=6.2.0 <6.4.0"
  wakelock_plus: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  drift_dev: ">=2.19.0 <2.23.0"
  build_runner: ^2.4.11

flutter:
  uses-material-design: true
